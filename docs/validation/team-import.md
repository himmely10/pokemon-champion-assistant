# 队伍截图导入验证（2026-09-11）

## 后续修复：根目录较小截图中的大狃拉

根目录 `能力.png`（2072×1181）和 `状态.png`（2073×1183）复现了不同于原图样本的失败：名称区域定位正确，但基础 OCR 与灰度检测均输出“大狂拉”，没有字典精确命中；身份未知又使特性、招式池无法关联。之前只通过较大原图，不能代表这一尺寸已验证。

修复只在名称尚未确认时启用：根据浅色文字边界去除紫色空白，分别读取紧裁原色与灰度反色图。两次结果一致、分数均不低于 0.85、且唯一精确匹配资料身份时才接受；没有设置“大狂拉→大狃拉”的强制替换。原始错误结果与补救证据均保留，界面仍提示局部重识别需要核对。

实际根目录两图均已识别为大狃拉，正确带入毒手、气势披带、击掌奇袭／毒击／近身战／佯攻，以及爽朗和培养点 2/32/0/0/0/32。证据在 `artifacts/team-ocr/root-debug/fixed-draft.json`；新增固定回归样本 `tests/fixtures/team_import/smaller/`，并测试两次读数不同、低分和未精确命中时继续拒绝。

修复后运行 `pytest tests/test_team_import.py tests/test_teams.py -q`：38 项通过（60.42 秒），包含原始大图、新增较小截图和整队保存／匹配回归。

输入：用户提供的两张原图，3184×1801 能力页、3185×1813 状态页，已保存在 `tests/fixtures/team_import/` 用于回归。

## 实际结果

按画面编号 1–6：沙奈朵、爱管侍（雌性）、煤炭龟、大狃拉、炽焰咆哮虎、幽尾玄鱼（雄性）。完整识别并人工对照通过：六只身份、36 项培养点、六个性格／特性／道具、24 招和 36 项面板数值。面板值保留证据，未接入战斗计算。

| 成员 | 培养点 HP/攻击/防御/特攻/特防/速度 | 性格 |
| --- | --- | --- |
| 沙奈朵 | 0/0/2/32/0/32 | 内敛 |
| 爱管侍（雌性） | 32/0/32/0/2/0 | 悠闲 |
| 煤炭龟 | 32/0/0/32/2/0 | 冷静 |
| 大狃拉 | 2/32/0/0/0/32 | 爽朗 |
| 炽焰咆哮虎 | 29/32/3/0/2/0 | 勇敢 |
| 幽尾玄鱼（雄性） | 0/32/1/0/1/32 | 爽朗 |

初始整图 OCR 将面板与培养点连读、将“大狃拉”读成“大狂拉”。最终通过字段裁剪、局部灰度增强和字典精确命中解决本例；模糊相似名称只作建议，不直接接受。局部重识别的较低置信度记录并提示人工核对。

队伍码局部 OCR 结果为 `FJROCNH887`，整图曾输出含 `0` 的不同结果。因此不宣称队伍码自动识别正确；界面提示易混淆字符并要求人工核对，可分别修改两页代码，原始证据仍保留。

## 测试与界面

- 完整测试运行：110 passed in 59.35s，包括真实 OCR 输入、配对失败拒绝、页面类型错误、空白及缺卡拒绝、模糊名字不自动采纳、性别缺失不猜形态、核对前不载入、人工修改代码、草稿保存重载、换图清除旧核对结果及原有整队匹配回归。
- 补充原图缩放和字段说明后，截图导入的 10 项测试再次通过（20.41 秒）；`pip check` 无依赖冲突；命令行用两张原图成功生成 `artifacts/team-ocr/draft.json`。
- Qt 实际渲染：`artifacts/team-ocr/import-review.png`。测试不会向用户队伍库自动写入示例队伍。
- 原始 OCR 与草稿开发输出：`artifacts/team-ocr/`。CLI 入口 `scripts/import_team_screenshots.py`。
- 当前 OCR 引擎的模型文件随安装包提供，在网络受限环境已完成识别。CPU 测试整组截图约 18 秒，具体耗时随硬件变化。

## 数据来源查询

公开 [Replica Team Viewer](https://champions.karthikb.dev/replica) 声明与 Pokémon 官方无关联。根据其页面公开表单，提交 `teamid` 做只读查询；HTTP 200 返回应用级 failure / 503，内容为服务修复中，不能查询新队伍。原始页面与响应在 `docs/research/2026-09-11-team-import/`。没有据此宣称官方 API 可用。

OCR 采用 [RapidOCR 官方文档](https://rapidai.github.io/RapidOCRDocs/main/en/install_usage/rapidocr/usage/) 的本地 Python 接口，依赖锁在 `requirements-ocr.txt`。

覆盖边界仅为本次队伍详情布局及样例，不能推广为所有视频或图片上的准确率保证；战斗选队的我方自动识别仍待后续实现。
