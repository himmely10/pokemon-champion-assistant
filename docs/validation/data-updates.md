# 自动资料更新验证记录

日期：2026-09-10。环境：项目 `.venv`，Windows，Python 3.13.5。

## 已实现与真实运行

- `check / sync / validate / rollback` 命令、中文控制台结果及按次 JSON 报告。
- OP.GG 完整图鉴公开数据解析、简体别名及特殊形态映射、六项种族值／属性和 Mega 关联；不导入使用率和伤害计算。
- 百科主列表与公开文件库补查、普通／闪光图标增量下载，现有文件先检查 SHA-256 后复用。
- 原始目录基线备份、不可变资料包、原子有效指针、跨进程更新锁、失败保留与回滚。
- 识别器固定版本和图标校验；新实例加载新版本，旧实例不被更新和回滚改写。新增无图记录不参与模板竞争。

真实联网 `check` 取得 OP.GG 352 个来源条目，发现本地新增 22 个物种、37 个形态资料，映射待处理为 0。第一次 `sync` 复用原有 722 张图标，并通过百科文件库补查下载 60 张新图标。主名单文章仍未增加对应行，证明仅重抓列表不能覆盖最新上传文件。

发布后有效目录为：

| 项目 | 结果 |
|---|---:|
| 物种 | 231 |
| 形态条目（含保留的历史外观） | 398 |
| 普通／闪光 PNG 文件 | 782 |
| 两种图标均齐全的形态 | 391 |
| 暂时只有资料的形态 | 7 |
| 已取得对应属性的条目 | 394 |

随后再次真实联网 `sync` 返回 `up_to_date`：新增 0、变更 0、图片请求 0，当前包 ID 不变。最后另行发布目录结构版本及历史来源说明的元数据修正，图标不重复下载。实际版本 ID 与完整操作历史见 `pokemon/current.json` 和 `pokemon/_reports/`。

## 自动验证

```powershell
.\.venv\Scripts\python.exe -X utf8 -m pytest tests -q
```

全套运行时 **41 项通过（28.70 秒）**：原有 13 项识别检查及当时新增的 28 项更新检查。随后补入“普通／闪光图标必须对应同一物种形态”回归用例；最终更新模块 **29 项通过（2.85 秒）**，并再次对实际发布包执行 `validate` 通过。最后改动仅涉及更新模块及来源校验，识别回归结果保持上述记录。

新增用例覆盖：实际完整网页解析、形态／主名称保持、截断／重复／非法种族值拒绝、缓存重验证与新增发现、重复同步、数据有而图标缺失、增量下载中断、异常大量移出阻止、路径与清单越界、进程锁、429 有限重试、文件库早于主名单上传、文件缺失与接口批次遗漏的区别、有效指针发布失败、当前索引损坏后的回滚，以及新旧识别器版本隔离。

真实 `validate` 对发布包内文件及所有图片执行校验，返回 398 形态／782 图标。测试中的回滚与故障注入发生在临时目录，不需要破坏用户实际资料来验证。

## 识别回归

使用新图标库执行现有命令行：

```powershell
.\.venv\Scripts\python.exe -X utf8 recognize_opponent.py "例子.png" --output-dir artifacts/opponent
```

结果仍为 **6/6**：苍炎刃鬼、风妖精、巨金怪、来悲粗茶、烈咬陆鲨、姆克鹰。匹配约 4.75 秒。HTML 模板预览与 JSON 使用同一已固定版本，结果包含 `dataset_id`。

这仍只有一张真实对战截图；391 个形态图标齐全表示可参与模板匹配，不代表 391 个形态都经过真实游戏识别评估。新图标未经过变形或转码。

## 明确保留的缺失与边界

以下 7 个形态未取得已核验的普通／闪光图标对，报告显示待就绪，不使用其他形态图片冒充：

- 霸王花（雌性）
- 火焰鸡（雌性）
- 姆克鹰（雌性）
- 颤弦蝾螈（低调的样子）
- 怒鹦哥（蓝羽毛）
- 怒鹦哥（黄羽毛）
- 怒鹦哥（白羽毛）

前三项的已知命名规则在文件库查询中没有完整图标对；其余四项尚未建立已核验的形态图标代码映射。这不等于宣称百科没有这些文件。

飘浮泡泡的雨水／太阳／雪云形态及莫鲁贝可空腹花纹保留历史种族值，当前未取得单独 OP.GG 属性条目；程序没有把普通形态属性套用过去。来源未列出不等于已退签；目前保留原图标与条目，合法性另待游戏规则核验。

本阶段没有新增 Qt 窗口、定时任务、双打统计接口、队伍配置表或伤害引擎；已有两份历史一次性脚本保留，但版本化目录会阻止它们覆盖新索引。
