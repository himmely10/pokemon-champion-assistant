# 队伍配置验证（2026-09-11）

实现覆盖 U2 的本地持久化与整队校验、U4 的我方手动阵容输入，以及 U12 的配置录入部分。未宣称完成 U6 我方识别、U12 能力值计算或 U13 伤害引擎。

## 证据

- 完整回归：`.venv-ui/Scripts/python.exe -X utf8 -m pytest -q`，99 项通过（原有 76 项 + 当时新增 23 项）。
- 随后追加真实主窗口新截图失效测试并补招式标签；队伍模块再次运行 24 项全部通过。
- Qt 离屏实际渲染并人工查看：`artifacts/team-preview/editor.png`，1060×800，六个成员标签、输入区、当前阵容区和匹配结果可见。
- 测试数据只写临时目录和 `artifacts`，没有往用户 `user_data` 中添加示例队伍。

## 核心回归

完整队伍 4/6 零引用；完整六只只有四只填写培养配置也不降级为部分登记；明确部分登记 4/4 仅引用四只、3/4 零引用；顺序变化；同种宝可梦跨队伍配置隔离；形态差异、身份歧义、重复身份及已知道具冲突整队拒绝；未知道具和明确无道具区分；未知培养点和零区分；单项与总预算边界；非法特性、性格、道具、重复招式与跨招式池输入；并发版本冲突；删除；未来数据库版本拒绝且文件字节不变。

界面测试覆盖保存重载、手动阵容独立于预存名单、配置修改立即撤销引用、自由搜索文字不沿用旧选择、切换成员身份清空旧培养配置、新截图清空当前确认但保留预存队伍。

## 来源与边界

`config/team_build_options.json` 从已存 OP.GG 计算器公开 HTML 提取：25 性格、374 特性中文定义、201 道具；培养点上限由该页面 `max="32"` 和预算 66 支持，记录来源及快照时间。这是当前录入规则的来源证据，尚未覆盖游戏内能力计算的独立验证。选项快照尚未接入自动更新。

保存的是队伍配置，不包含临时 HP、能力等级、顺风等战斗状态。匹配结果只供校验视图展示；下一步接入我方识别及计算时必须复用 `match_team` 整队门槛，禁止按物种直接读取库。
