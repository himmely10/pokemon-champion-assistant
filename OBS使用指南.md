# OBS 与 Switch 采集使用指南

本项目通过 OBS WebSocket 读取视频源截图。**仅打开 OBS 不代表已经启用 WebSocket。** 需要先启用服务器，再从资料台连接。

## 1. 接好采集卡与游戏画面

1. Switch 底座 HDMI 输出连接采集卡，采集卡 USB 连接电脑。
2. 打开 OBS，在「来源」区域点击 `+`，添加「视频采集设备」，选择你的采集卡。
3. 确认 OBS 预览能看到 Switch 画面。建议使用完整 16:9 游戏画面，避免裁切、拉伸和额外黑边。
4. 进入 **Pokémon Champions 对战的选队界面**，右侧应显示对手六只宝可梦。

当前识别器不支持 Pokémon HOME 盒子、游戏主菜单或战斗中其他布局。这些画面能采集成功，但不应被当成对手队伍识别。

## 2. 启用 OBS WebSocket

1. 打开 OBS 顶部菜单「工具 → WebSocket 服务器设置」。
2. 勾选「启用 WebSocket 服务器」。
3. 记下服务器端口，默认 `4455`。
4. 保持身份验证开启，使用此窗口的服务器密码。
5. 点击「应用」，然后「确定」。

OBS 28 及以上内置 WebSocket，通常不需要额外安装插件。菜单和按钮译名可能随 OBS 版本略有不同，见 [OBS 官方远程控制说明](https://obsproject.com/kb/remote-control-guide)。

## 3. 在本项目中连接

1. 双击项目根目录的 `run_assistant.bat`。
2. 点击左侧「连接 OBS」。
3. OBS 与程序在同一台 Windows 电脑时，可点击 **「读取本机 OBS 设置」**：程序从标准 OBS 配置位置读取端口与密码，只用于当前运行，不修改 OBS。
4. 如果使用便携版、远程 OBS，或者启动 OBS 时用命令行覆盖了连接参数，请手动填写实际设置：地址只写主机名或 IP，端口单独填写。不要把 `ws://127.0.0.1:4455` 整串填入地址。
5. 点击「连接并读取采集源」。成功后，选中 Switch 的「视频采集设备」，点击「使用此采集源」。
6. 回到主界面，点击「从 OBS 截图并识别」。每点击一次采集一帧，不持续识别视频。

同机推荐地址 `127.0.0.1`。也支持读取场景，但场景应只包含完整游戏画面；建议直接选视频采集设备，避免直播装饰、字幕或变换影响固定位置裁剪。纯音频输入会从列表中过滤。

## 4. 如何判断成功

- 连接窗口显示 OBS 版本并列出视频源：说明服务器连接与认证成功。
- 主界面左侧出现截图并显示分辨率：说明真实截图已取得。
- 右侧六个槽位出现名字：说明这张选队画面已完成识别。
- 截图正常但显示「待确认」：检查是否为正确选队布局，再考虑手动修正；这与连接失败是不同的问题。

最近一次成功完成分析的输入类型、源名称与识别报告位于 `artifacts/desktop/last_result.json`。该报告不包含密码。

## 5. 常见问题

| 现象 | 处理方法 |
|---|---|
| OBS 已开，但端口拒绝连接 | 先勾选 WebSocket 服务器并应用；核对端口。开启 OBS 主窗口本身不会启用服务器。 |
| 密码验证失败 | 使用 WebSocket 服务器密码；同机可重新点「读取本机 OBS 设置」。 |
| 找不到本机设置 | 便携版或其他系统请手动填写；标准 Windows 位置为 `%APPDATA%/obs-studio/plugin_config/obs-websocket/config.json`。 |
| 读取本机设置后仍失败 | 检查 OBS 是否应用了新设置、是否有多个 OBS 实例，或者命令行启动参数覆盖了端口／密码。 |
| 地址无法解析 | 同机改为 `127.0.0.1`，不要填写协议前缀和端口。远程连接使用运行 OBS 的电脑地址。 |
| 超时 | 核对地址、端口、主机是否可达以及视频源状态。 |
| 有源列表，但截图请求失败 | 重新读取源列表，选择仍存在且有画面的实际视频源；场景切换、源重命名后需重新选择。 |
| 截图全黑或显示无信号 | 先在 OBS 检查采集卡、HDMI 和视频设备；资料台读取的是 OBS 当前源的真实内容。 |
| 有截图但六只都待确认 | 切到 Champions 对战选队界面；HOME 盒子和主菜单不适用。检查源是否带黑边、裁切或装饰。 |

## 6. 密码与设置保存

程序仅保存主机、端口、源名称到 `config/local_ui.json`，不保存密码。重启程序后需要重新输入密码或点击读取本机设置。不要把 OBS 配置文件或密码粘贴进聊天、截图和公开问题报告。

本程序使用的 OBS 请求只读取版本、源列表、场景列表与截图，不会开始推流、录制或切换场景。

## 本机验证记录

2026-09-11：确认最初无法连接的原因是 WebSocket 未启用。用户启用并应用后，已连接 OBS 32.2.2，并从「视频采集设备」取得 3840×2160 的实际 Switch 画面。当时 Switch 位于 Pokémon HOME 盒子，六槽位均未识别，没有将盒子中的精灵误作为对手结果。真实 Champions 选队识别仍需在正确界面继续验证；项目根目录示例的六只识别测试已通过。
