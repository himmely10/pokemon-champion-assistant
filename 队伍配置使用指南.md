# 我方队伍配置

双击 `run_assistant.bat`，点击主窗口顶部的 **我方队伍配置**。

如果有游戏队伍详情的能力／状态两张截图，可点击 **截图导入** 自动生成待核对草稿，减少手动录入，详见 [截图导入队伍指南](截图导入队伍指南.md)。

## 保存一支队伍

1. 点击 **新建**，填写队伍名称。
2. 默认使用 **完整登记**，在六个成员标签中分别选择宝可梦和具体形态。可以输入简体中文名称搜索，但必须选择下拉列表中的完整条目。
3. 为成员填写六项培养点、性格、特性、道具和四个招式。允许先保存成员名单，之后补配置。
4. 点击窗口固定底栏的 **保存队伍**，无需滚动寻找按钮。之后从顶部列表选择即可继续编辑；**另存副本**可建立同种宝可梦使用不同配置的另一支队伍。

培养点为空时会显示具体字段，例如“HP 未填写”“速度未填写”，与明确填写 `0` 不同；性格、特性、道具和四个招式槽也分别指出未填写项。通过输入框减到最小值可恢复未知。当前输入规则为单项 0–32、已填总和不超过 66，采用 [OP.GG 计算器](https://op.gg/zh-cn/pokemon-champions/calculator) 的输入上限和预算；使用 Champions 培养点单位，不接受传统 252/510 努力值。

性格展示能力增减，特性按当前形态选择；招式按当前形态的可用招式池提供，不允许重复。招式列表附属性、威力和命中率，悬浮可看说明。未填招式保留未知槽位。

道具的“未知 / 未填写”与“无道具（已确认）”不同。缺失资料的条目会保留标识、阻止错误保存，不会悄悄替换成别的配置。此版本执行输入校验，尚不是赛季完整参赛合法性校验器。

## 重命名与保存提示

点击顶部 **重命名** 输入新名字，再点击底部 **保存队伍**。名称和当前编辑一并保存，保留原队伍 ID；不会新建副本或丢失成员配置。

保存成功后会检查伤害计算的缺项与引擎支持情况。有问题时弹出感叹号提示，指出成员、招式或未确认的条件，例如“沙奈朵：复制到的特性未确认”。培养点未知、性格／道具未填或尚未支持的特殊招式都会给出说明。合法的未完成配置仍会保存；非法输入则显示“队伍未保存”。

窗口按当前显示器可用大小打开。培养配置和整队验证分成两个页签，内容可滚动，保存按钮始终在底栏。

## 当前阵容与整队匹配

伤害计算现已改为直接选择预存队伍和成员，**不需要填写当前阵容或通过匹配**。培养配置按已保存的队伍读取，修改后须保存才会生效。

**整队匹配验证（可选）** 页签 独立于预存名单，保留以下规则供后续自动识别接入。需要验证时，可对照游戏画面选择六个位置的身份及道具。当前自动识别仍针对右侧对手，尚未自动识别我方及我方道具。

| 预存方式 | 当前对应情况 | 是否引用配置 |
| --- | --- | --- |
| 完整登记六只 | 六只全部对应，顺序不同也可以 | 引用这支队伍的六只配置 |
| 完整登记六只 | 仅四只相同 | 整队拒绝，零引用 |
| 完整名单六只，只补完四只配置 | 仅四只相同 | 整队拒绝，仍按六只名单匹配 |
| 用户明确选择部分登记四只 | 这四只全部唯一对应 | 仅引用这四只，其他槽位保持未知 |
| 用户明确选择部分登记四只 | 只对应三只 | 整队拒绝，零引用 |
| 任一登记方式 | 已确认道具与预存道具冲突 | 整队拒绝，零引用 |

重复身份、未确认的战斗形态不会作为有效匹配依据；来悲粗茶和彩粉蝶按既有外观合并策略处理。Mega 和有对战差异的形态继续独立。

用户选择哪支预存队伍，就只校验哪支队伍；不会跨队伍按物种拼凑配置。匹配通过后，验证页下方显示本次允许引用的配置。编辑未保存内容、切换预存队伍、修改当前阵容会立即清除或重新计算引用；读取新截图或 OBS 截图会清空当前我方阵容确认，预存队伍仍然保留。

主界面会记住本局选中的预存队伍，并将它传给双向伤害计算。招式资料旁可查看当前成员按培养点、性格、速度等级、顺风和条件特性计算的实配速度；对手同时显示参考速度线。

## 本地文件与备份

- 用户数据：项目根目录 `user_data/teams.sqlite3`。关闭软件后复制该文件即可备份。
- 队伍以独立 ID 保存，成员配置嵌入其所属队伍；每次保存递增版本，避免旧窗口覆盖新版本。
- 自动更新宝可梦资料和采用率不会改写用户队伍库。新资料若不再能验证旧身份或招式，匹配会拒绝引用，等待人工修正。
- 数据库当前版本为 1；遇到更高版本会拒绝读取并保留原文件。此项目此前没有旧版队伍配置库，因此没有猜测迁移未知格式。
- 性格、特性中文名称及道具选项来自 `config/team_build_options.json`，记录了 2026-09-10 的 OP.GG 计算器快照和来源。当前这些选项是随程序提供的静态快照，尚未接入每日采用率更新；宝可梦身份、特性归属及招式池读取现有版本化资料。

软件不需要账号登录即可保存本地队伍。


## v0.2 安装版补充

安装版把队伍库放在个人数据目录，与安装目录和公共资料分开。程序升级和默认卸载均保留该目录。首次引导可导入旧项目；已有个人库时不覆盖，原文件保留。伤害计算继续使用已保存的命名队伍，整队匹配规则不变。
