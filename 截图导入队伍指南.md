# 从能力／状态截图导入队伍

打开软件 → **我方队伍配置** → **截图导入**。

1. 选择同一队伍的 **能力截图** 和 **状态截图**，点击 **识别两张截图**。过程在本地运行，不上传图片。
2. 对照原图核对两页队伍码和六只成员身份。点击 **放大查看当前原图** 可缩放、滚动查看文字。
3. 特别核对 `0/O`、`1/I`，以及爱管侍、幽尾玄鱼的性别形态。两页队伍码或对应身份不同，程序拒绝合并。纠正身份会清空依赖旧身份的特性与四招，避免沿用错误结果。
4. 勾选核对复选框，点击 **载入为新队伍草稿**，在原队伍编辑器补齐问号、核对配置，最后 **保存队伍**。

导入不覆盖其他预存队伍，也不把预存名单当作当前我方阵容。保存后继续遵守整队匹配规则。取消识别或核对会保留之前编辑的队伍。

## 读取范围

| 页面 | 字段 | 处理方式 |
| --- | --- | --- |
| 两页共同 | 队伍码、名称、部分性别标记 | 两页分别识别；队伍码用于配对和来源记录，未经线上验证 |
| 能力 | 特性、道具、四招 | 与本地字典及形态招式池校验，不确定时保留未知 |
| 状态 | 六项培养点、六项面板数值 | 分开裁剪，避免把数值和培养点连读 |
| 状态 | 性格上升／下降箭头 | 按颜色映射性格，无明确箭头时保持未知，不猜中性性格 |

培养点与性格写入草稿；面板值目前只保留为来源证据，悬浮培养点单元格可查看。尚未把面板值接入伤害计算，也不会用它反推不明确的培养点。

保存的来源证据包括原始 OCR 文字、分数、局部重识别结果、卡片坐标、文件摘要及人工身份修正。原图不嵌入队伍数据库，请自行保留截图。OCR 分数不是正确率保证。

## 图片要求与限制

- 当前支持样例的 **两列三行、六张紫色卡片** 队伍详情布局，以及 PNG、JPG、WebP 图片。
- 可以用视频截图，应尽量清晰、完整、正视。卡片外的字幕通常不影响定位；遮挡字段的弹幕、头像、字幕或剪裁可能导致待确认或拒绝导入。
- 本轮完整验证来自用户提供的两张原图，尚未评估所有分辨率、语言及压缩程度。
- 尚不支持直接读取视频文件或自动翻页。先保存两张截图再导入；现有 OBS 对手识别功能保留。
- 身份无法确认时，可在核对表分别选择两页身份；其余字段在队伍编辑器补全。

## 安装与命令行

当前项目 `.venv-ui` 已安装 OCR。新环境执行：

```powershell
.\.venv-ui\Scripts\python.exe -m pip install -r requirements-ocr.txt
```

采用 RapidOCR 3.9.2、ONNX Runtime 1.30.0 CPU 引擎，模型随当前包提供。安装需要联网，已安装后识别可离线运行，无需云端账号或密钥。

生成 JSON 草稿：

```powershell
.\.venv-ui\Scripts\python.exe -X utf8 scripts/import_team_screenshots.py --ability "能力.png" --status "状态.png"
```

默认写入 `artifacts/team-import/draft.json`，不写预存队伍库。`import_source.review_required` 提醒调用方核对后使用；当前桌面直接识别图片，尚未提供任意 JSON 文件导入。

## 直接输入队伍码

截至本次核查，没有确认到官方公开、可供软件调用的队伍查询 API 文档。

找到 [第三方 Replica Team Viewer](https://champions.karthikb.dev/replica)，其公开表单接受 `teamid`。本次查询返回应用级 `503`，表示服务器修复中、暂时不能查询新队伍。因此本版没有接入队伍码在线查询。原始响应保存在 `docs/research/2026-09-11-team-import/replica-response.html`（内容为 JSON）。

待服务恢复后可继续验证，把它作为另一种草稿导入方式，并复用同一套保存和整队匹配规则。
